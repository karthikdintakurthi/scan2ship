# ========================================
# SCAN2SHIP - ENVIRONMENT TEMPLATE
# ========================================
# 
# ⚠️  SECURITY WARNING ⚠️
# This is a template file. Copy it to .env.local and replace ALL placeholder values
# with your actual production secrets. NEVER commit .env.local to version control!
#
# To generate secure secrets:
# - JWT Secret: openssl rand -base64 32
# - Database Password: openssl rand -base64 16
# - Webhook Secrets: openssl rand -hex 32
#
# ========================================

# Database Configuration
# Replace with your actual database connection string
DATABASE_URL="postgresql://username:password@localhost:5432/database_name"

# Authentication & Security
# IMPORTANT: Generate a strong, random JWT secret (minimum 32 characters)
# You can generate one using: openssl rand -base64 32
JWT_SECRET="your-super-secure-jwt-secret-here-minimum-32-characters"

# Application Configuration
NODE_ENV="development"
# Replace with your application name
NEXT_PUBLIC_APP_NAME="Your Application Name"
# Replace with your actual domain (use https:// in production)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Delhivery API Configuration
# Note: DELHIVERY_API_KEY is now managed at client level through pickup locations
DELHIVERY_BASE_URL="https://track.delhivery.com"
# Generate with: openssl rand -hex 32
DELHIVERY_WEBHOOK_SECRET="your_delhivery_webhook_secret_here"

# OpenAI API Configuration (for address processing)
OPENAI_API_KEY="your_openai_api_key_here"
OPENAI_MODEL="gpt-4o-mini"

# WhatsApp functionality has been removed from this application

# Note: Google Cloud configuration removed as it's no longer needed

# Note: Email configuration removed as it's no longer needed

# File Upload Configuration
MAX_FILE_SIZE="5242880"  # 5MB in bytes
ALLOWED_FILE_TYPES="image/jpeg,image/png,image/gif"

# Rate Limiting
RATE_LIMIT_WINDOW="900000"  # 15 minutes in milliseconds
RATE_LIMIT_MAX_REQUESTS="100"

# Logging Configuration
LOG_LEVEL="info"
LOG_FILE_PATH="./logs/app.log"

# Development Tools
NEXT_TELEMETRY_DISABLED="1"
# Only enable debug in development - NEVER in production
DEBUG="app:*"

# ========================================
# SECURITY CHECKLIST
# ========================================
# Before deploying to production, ensure:
# 1. All placeholder values are replaced with real secrets
# 2. JWT_SECRET is at least 32 characters long
# 3. Database credentials are secure and unique
# 4. API keys are valid and have appropriate permissions
# 5. NODE_ENV is set to "production"
# 6. DEBUG is disabled or removed
# 7. All URLs use HTTPS in production
# 8. File .env.local is in .gitignore and never committed
#
# ========================================
# SECRET GENERATION COMMANDS
# ========================================
# JWT Secret: openssl rand -base64 32
# Database Password: openssl rand -base64 16
# Webhook Secret: openssl rand -hex 32
# API Key (if needed): openssl rand -hex 24
#
# ========================================


